const { Given, And, When, Then } = require('cucumber');

Scenario('Una guía personalizada de lectura', function () {
    Given('un padre o maestro que utiliza la aplicación de comprensión lectora.', function () {});
    And('cuando uso la aplicación.', function () {});
    When('veo las recomendaciones de libros relacionados con los temas de interés de mi hijo y adaptados a su nivel de lectura.', function () {});
    Then('podré fomentar la exploración de diferentes géneros literarios.', function () {});

});

/*
Input:
- entrar a recomendaciones
Output:
- mostrar recomendaciones
*/
